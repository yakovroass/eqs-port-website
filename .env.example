# Copy to .env and adjust. Do not commit .env.

# SQLite (path relative to project root)
DATABASE_URL="file:./dev.db"

# First admin user (used by: npm run db:seed)
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="change_this_min_8_chars"
